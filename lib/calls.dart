import 'package:flutter/material.dart';

class callex extends StatelessWidget {
  const callex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Color.fromARGB(255, 201, 181, 218),
    );
  }
}